# Lecture 6 Demo — Testing Legacy Code That Has No Tests

**Time:** ~8 minutes | **Tool:** Claude Code

## What This Proves

AI can generate meaningful test coverage for legacy code that has zero tests — showing before/after coverage, risk assessment for untested paths, and confidence scoring for generated tests.

## Run the Demo

Open any terminal. Type `claude`. Paste this:

> Find the cio-ai-demos repo on my machine. Inside it, go to course-12-legacy-modern/lecture-06-test-generator. Build a Test Coverage Generator Dashboard with 6 modules showing before/after coverage, generated test counts, and risk heatmap for untested code. Generate index.html and open in browser.

## Keep Going — Paste These Next

1. "Generate integration tests for the 3 most critical business workflows"
2. "Build a regression test suite that runs before every legacy system change"
3. "Create a test debt burndown chart with weekly targets"

## CIO Takeaway

> "No tests doesn't mean untestable. AI can generate meaningful test coverage for legacy code in minutes, not months."
