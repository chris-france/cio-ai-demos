# Lecture 3 Demo — The Strangler Fig Pattern

**Time:** ~8 minutes | **Tool:** Claude Code

## What This Proves

You can visualize the strangler fig migration pattern — tracking how a new system gradually replaces the old one, feature by feature, with rollback safety at every step.

## Run the Demo

Open any terminal. Type `claude`. Paste this:

> Find the cio-ai-demos repo on my machine. Inside it, go to course-12-legacy-modern/lecture-03-strangler-fig. Build a Strangler Fig Migration Tracker with 8 features showing migration status, traffic split between old and new systems, and rollback readiness. Generate index.html and open in browser.

## Keep Going — Paste These Next

1. "Add a traffic routing visualization showing the proxy layer splitting requests"
2. "Build a rollback playbook for each migrated feature"
3. "Create a weekly migration status report template for stakeholders"

## CIO Takeaway

> "The strangler fig pattern lets you replace legacy systems without a big bang cutover. Every feature migrated is a risk eliminated."
