# Lecture 8 Demo — Zero Trust Architecture

**Time:** ~8 minutes | **Tool:** Claude Code

## What This Proves

AI generates a complete zero trust policy set tailored to your organization's size, industry, and current architecture — covering network micro-segmentation, identity (MFA + RBAC), data protection (encryption + DLP), and device posture checking — with a phased implementation roadmap.

## Run the Demo

Open any terminal. Type `claude`. Paste this:

> Find the cio-ai-demos repo on my machine. Inside it, go to course-05-security-ops/lecture-08-zero-trust. Build a Zero Trust Policy Generator. Input form: org size, industry, current architecture. Generate policies for 4 pillars: network (micro-segmentation), identity (MFA + RBAC), data (encryption + DLP), devices (posture checking). Include priority levels, recommended tools, and a 4-phase implementation roadmap with timeline. Create index.html and open in browser.

## Keep Going — Paste These Next

1. "Generate specific firewall rules for the network segmentation — VLANs and ACLs for an engineering firm"
2. "Create a Microsoft Entra Conditional Access policy set in JSON format for the identity pillar"
3. "Build a compliance mapping showing how these zero trust controls satisfy SOC 2 and NIST requirements"

## CIO Takeaway

> "The perimeter is dead. Zero Trust is not a product you buy — it is 20 policies you implement. AI generates the policies, the roadmap, and the specific configurations in minutes."
